SIMONO OPTIMIZED BUILD

Upload every file and folder in this directory to the web root. Keep the assets folder structure unchanged.

Optimizations applied:
- Embedded founder photo extracted and converted to WebP with lazy loading.
- Embedded logo extracted as a cached PNG asset.
- Cal.com loads only after the first booking action.
- Fourteen CSS blocks consolidated in their original cascade order.
- Google Fonts made non-render-blocking while retaining display=swap and fallbacks.
- Main interaction JavaScript moved to a deferred cached asset.
